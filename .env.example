# Google Nest API Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
OAUTH_CLIENT_ID=your_client_id_here.apps.googleusercontent.com

# Device Access Project ID
# Get this from: https://console.nest.google.com/device-access/
PROJECT_ID=your_project_id_here

# Application Settings
CHECK_INTERVAL=5
CONFIDENCE_THRESHOLD=0.5

# eBird API (optional, for location-based species filtering)
# Get a key from: https://ebird.org/api/keygen
# Region code: e.g., US-NM for New Mexico, US-CA for California
EBIRD_REGION_CODE=US-NM

# Note: Sensitive credentials (OAuth client secret, access tokens, refresh
# tokens, device ID, eBird API key) are stored in the OS keychain, not in
# this file. They are set up by tools/setup_credentials.py.
